# Nginx reverse proxy configuration for EHRJ Madrasha ERP & Public Website
# Save to /etc/nginx/sites-available/ehrjmadrasha.edu.bd

server {
    listen 80;
    server_name ehrjmadrasha.edu.bd www.ehrjmadrasha.edu.bd;

    # Redirect all HTTP traffic to HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name ehrjmadrasha.edu.bd www.ehrjmadrasha.edu.bd;

    # SSL Certificates (Managed by Certbot / Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/ehrjmadrasha.edu.bd/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ehrjmadrasha.edu.bd/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";
    add_header Referrer-Policy "strict-origin-when-cross-origin";

    # Gzip Compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Frontend Reverse Proxy (Next.js Port 3000)
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API Reverse Proxy (Express API Port 3001)
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static File Uploads Direct Serving with Cache Headers
    location /uploads/ {
        alias /var/www/ehrjmadrasha/backend/uploads/;
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }
}
